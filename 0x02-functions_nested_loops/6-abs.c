#include <stdlib.h>
#include "main.h"

/**
 * _abs - returns a the absolute value of an integer
 * @n: number to be checked
 *
 * Return: Always 0 (Success)
 */

int _abs(int n)
{
int a;
  
a = abs(n);  
return (a);
}
