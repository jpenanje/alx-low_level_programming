#include "main.h"

/**
 * puts2 - print characters
 * @str: pointer to string
 *
 * Return nothing
 */

void puts2(char *str)
{
int l;
  
l = 0;
while (s[l] != '\0')
{
if (l % 2 == 0)
{
_putchar(s[l]);
}
l++;
}
}
