#include "main.h"
#include <string.h>

/**
 * _strcpy - copy string to a desination
 * @dest: string destination
 * @src: string source
 *
 * Return: Always 0 (Success)
 */

char *_strcpy(char *dest, char *src)
{
return (strcpy(dest, src));
}
