#include "main.h"
#include <string.h>

/**
 * _strstr - pointer
 * @haystack: pointer
 * @needle: substring
 *
 * Return: pointer to the beginning
 */

char *_strstr(char *haystack, char *needle)
{
return (strstr(haystack, needle));
}
