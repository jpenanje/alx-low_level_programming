#include "main.h"

/**
 * is_prime_number - prime number or not
 * @n: number
 * Return: 1 if prime number else 0
 */

int is_prime_number(int n)
{
if ((n % 2) != 0)
{
return (1);
}
else
{
return (0);
}
}
