#include "main.h"
#include <string.h>

/**
 * _strchr - locates a character
 * @s: pointer
 * @c: character
 *
 * Return: pointer to first occurrence
 */

char *_strchr(char *s, char c)
{
return (strchr(s, c));
}
