#include "main.h"
#include <string.h>

/**
 * _strpbrk - number of bytes
 * @s: pointer
 * @accept: substring
 *
 * Return: pointer
 */

char *_strpbrk(char *s, char *accept)
{
return (strpbrk(s, accept));
}
